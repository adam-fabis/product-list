import React from 'react';
import PropTypes from 'prop-types';
import Navigation from './components/Navigation';

const App = (props) => {
  return (
    <Navigation />
  );
};

App.propTypes = {

};

export default App;
